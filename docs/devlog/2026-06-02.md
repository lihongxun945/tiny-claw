# 2026-06-02

## Agent Loop 与 Gateway 集成测试

- `AgentSession` 支持注入 `ModelClient`，生产环境默认行为保持不变。
- 新增脚本化 `FakeModelClient`，覆盖直接回复、工具回环、未知工具、工具异常、模型异常和最大迭代次数。
- 新增真实 Gateway HTTP 集成测试：使用临时 workspace 和随机端口启动 gateway 子进程。
- 覆盖配置递归脱敏、Memory API CRUD、会话列表过滤、持久化会话删除和 WebUI 静态代理。

## WebUI E2E

- 引入 `@playwright/test` 和 Chromium E2E。
- 覆盖 Markdown 表格渲染、记忆编辑与启停、Memory API 错误提示。
- `npm run test:all` 现在执行类型检查、coverage、WebUI build 和 Playwright E2E。
- CI 在执行测试前安装 Chromium。
